---
# tasks file for docker

 - name: create docker image
   docker_image:
      name: oussemaaaa/projetCD
      build:
         path: /var/lib/jenkins/workspace/projectCD                
         pull: no
      source: build
      state: present
      force_source: true
 - name: run docker container
   docker_container:
      name: cd-container
      image: oussemaaaa/projetCD:latest
      state: started
      recreate: true
      ports:
        - "4200:80"
